mixin addAdmin()
	.well.well-sm
		form.form-horizontal(action="/admin/" method="POST")
			h3 Add Admin
			.form-group
				label.col-sm-3.control-label.d-none.d-sm-block(for="name") Name
					.col-sm-9.col-xs-12
						.input-group
							span.input-group-addon @
							input.form-control(type="text" name="name" placeholder="🗿")
			.form-group
				label.col-sm-3.control-label.d-none.d-sm-block(for="email") Email Address
					.col-sm-9.col-xs-12
						.input-group
							span.input-group-addon @
							input.form-control(type="email" name="email" placeholder="✉")
			.form-group
				label.col-sm-3.control-label.d-none.d-sm-block(for="adminLevel") Admin Level
					.col-sm-9.col-xs-12
						select.form-control
							- const options = [1,2,3]
							each opt in options
								option #{opt}
			.form-group
				label.col-sm-3.control-label.d-none.d-sm-block(for="password") Password
				.col-sm-9.col-xs-12
					.input-group
						span.input-group-addon @
						input.form-control(type="password" name="password" placeholder="🔑")
			.form-group
				label.col-sm-3.control-label.d-none.d-sm-block(for="password-confirm") Password
				.col-sm-9.col-xs-12
					.input-group
						span.input-group-addon @
						input.form-control(type="password" name="password-confirm" placeholder="🔑")
			
